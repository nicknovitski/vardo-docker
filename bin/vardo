#!/bin/bash
docker_enter() {
  [ -f /var/lib/boot2docker/nsenter ] || docker run --rm -v /var/lib/boot2docker/:/target jpetazzo/nsenter
  sudo /var/lib/boot2docker/docker-enter "$@"
}

docker_enter $1 su - dev
